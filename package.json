{
  "name": "corewar-stadium-service",
  "version": "0.1.0",
  "description": "The coreware stadium service",
  "main": "app/corewar_stadium_service.js",
  "scripts": {
    "serve": "forever start -p logs/ -l forever.log -a -o logs/out.log -e logs/err.log app/corewar_stadium_service.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Simon Relet",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.14.2",
    "express": "^4.13.3",
    "fs-promise": "^0.3.1",
    "mkdirp": "^0.5.1"
  }
}
