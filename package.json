{
  "name": "corewar-stadium-service",
  "version": "0.5.0",
  "description": "The coreware stadium service",
  "main": "app/corewar_stadium_service.js",
  "scripts": {
    "clean": "rm -fr logs",
    "dist-clean": "npm run clean node_modules",
    "start": "mkdir -p logs && forever start -p logs/ -l forever.log -a -o logs/out.log -e logs/err.log app/corewar_stadium_service.js",
    "stop": "forever stop app/corewar_stadium_service.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Simon Relet",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.14.2",
    "express": "^4.13.3",
    "fs-promise": "^0.5.0",
    "request-promise": "^2.0.1",
    "ursa": "^0.9.3"
  }
}
